// Monodrone Fighter

BEGIN ~a7#dmdrm~

IF ~True()~ 1
  SAY @3003 /* This modron has a spherical body with only a single eye. Two arms and two legs are sprouting from its sides. It doesn't appear to notice you. */
  IF ~~ EXIT
END
