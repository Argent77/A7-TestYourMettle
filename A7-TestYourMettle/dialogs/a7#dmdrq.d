// Quadrone Fighter (melee)

BEGIN ~a7#dmdrq~

IF ~True()~ 1
  SAY @3004 /* This modron has a cube-shaped body with two arms, two legs, and two small wings sprouting from its sides. On the front of the cube is an animated face. It is ignoring you completely. */
  IF ~~ EXIT
END
