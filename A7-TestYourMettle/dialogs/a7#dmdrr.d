// Quadrone Fighter (ranged)

BEGIN ~a7#dmdrr~

IF ~True()~ 1
  SAY @3005 /* This modron has a cube-shaped body with four arms and two legs. On the front of the cube is an animated face. It is ignoring you completely. */
  IF ~~ EXIT
END
